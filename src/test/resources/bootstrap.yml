spring:
  cloud:
    config:
      enabled: false
      server:
        git:
          uri: https://github.com/RichardCSantana/configserver-repo.git
  application:
    name: configserver
eureka:
  client:
    enabled: false
